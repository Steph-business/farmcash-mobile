// Placeholder — les vrais tests viendront avec les écrans une fois stabilisés.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Placeholder', () {
    expect(1 + 1, equals(2));
  });
}
